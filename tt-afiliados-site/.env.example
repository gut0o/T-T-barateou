# Mercado Livre
ML_CLIENT_ID=
ML_CLIENT_SECRET=
ML_REDIRECT_URI=https://t-t-barateou.vercel.app/api/callback

# Etapa 5 - criptografia dos tokens.
# Gere 32 bytes em Base64. Exemplo de comando:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ML_TOKEN_ENCRYPTION_KEY=

# Adicionada automaticamente quando um Vercel Blob privado
# é criado/conectado ao projeto.
BLOB_READ_WRITE_TOKEN=
